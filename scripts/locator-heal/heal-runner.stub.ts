/**
 * Locator-heal runner stub.
 * Wire to your real heal orchestrator when `LOCATOR_HEAL_ENABLED=true`.
 */
console.log(
  '[locator-heal stub] No-op. Implement heal orchestration in this script when ready.'
);
